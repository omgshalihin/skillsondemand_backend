debug: true
spring:
  data:
    mongodb:
      uri: mongodb+srv://${MONGO_USER}:${MONGO_PWD}@cluster0.fhnnnvj.mongodb.net/${MONGO_DB}
      database: ${MONGO_DB}
  logging:
    level:
      org:
        springframework:
          boot:
            autoconfigure: ERROR

